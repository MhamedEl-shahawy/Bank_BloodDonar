<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/login.css">
    <script type="text/javascript" src="/js/login.js"></script>

</head>

<body>
    <main>
        <% if (errorMessage) { %>
            <div class="user-message user-message--error"><%= errorMessage %></div>
        <% } %>
            <img class="wave" src="/images/logwave.png">
    <div class="container">
        <div class="img">
            <img src="/images/./logbg.svg">
        </div>
        <div class="login-content">
            <form  class="login" action="/adminLogin" method="POST">
                <img src="/images/logavatar.svg">
                <h2 class="title">Welcome</h2>
                <div class="input-div one">
                   <div class="i">
                        <i class="fas fa-user"></i>
                   </div>
                   <div class="div">
                        <h5>ID</h5>
                        <input
                           class="input <%= validationErrors.find(e => e.param === 'adminID') ? 'invalid' : '' %>"
                           type="text" 
                           name="adminID" 
                           id="adminID" 
                           value="<%= oldInput.adminID %>">
                   </div>
                </div>
                <div class="input-div pass">
                   <div class="i"> 
                        <i class="fas fa-lock"></i>
                   </div>
                   <div class="div">
                        <h5>Password</h5>
                        <input type="password" 
                           class="<%= validationErrors.find(e => e.param === 'password') ? 'invalid' : '' %>" 
                           name="password" 
                           id="password" 
                           value="<%= oldInput.password %>">
                   </div>
                </div>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <input type="submit" class="btn" value="Login">
                
            </form>
        </div>
    </div>
    </main>
<%- include('../includes/end.ejs') %>