<%- include('../includes/head.ejs')%>
  </head>
  <body>
    <%-include('../includes/userNavigation.ejs')%>
    <main>
      <div class="main__info">
        <!-- <h2 class="header">My Account</h2> -->
        <p>
          Update your account at any time by clicking the "Edit" button below.
        </p>
        <form class="form-user" action="">
          <% users.forEach(user => { %>
              <% if (isAuthenticated && user._id.toString() === currentUser._id.toString()) { %>
            <div class="form-group">
              <label for="firstname" class="control-label">First Name:</label>
              <label for="firstname" class="value"><%= user.firstName%></label>
            </div>
            <div class="form-group">
              <label for="lastname" class="control-label">Last Name:</label>
              <label for="lastname" class="value"><%= user.lastName %></label>
            </div>
            <div class="form-group">
              <label for="username" class="control-label">User Name:</label> <!-- <%= user.firstName.concat('',user.lastName)%> -->
              <label for="username" class="value"><%= user.firstName.concat(' ',user.lastName)%></label>
            </div>
            <div class="form-group">
              <label for="PrimaryEmail" class="control-label"
                >Primary Email:</label
              >
              <label for="PrimaryEmail" class="value"><%= user.email%></label>
            </div>
            <div class="form-group">
              <label for="enableAvatar" class="control-label"
                >City:</label
              >
              <label for="enableAvatar" class="value"><%= user.city%></label>
            </div>
            <div class="form-group">
              <label for="enableAvatar" class="control-label"
                >Phone:</label
              >
              <label for="enableAvatar" class="value"><%= user.phoneNumber%></label>
            </div>
            <div class="form-group">
              <label for="avatarPreview" class="control-label"
                >Blood Type:</label
              >
              <label for="avatarPreview" class="value"><%= user.bloodType %></label>
            </div>
            <a class="btn" href="/donor/<%= user.id %>/edit"><span>Edit</span></a>
            <a class="btn-back" href="/donor/home/index">Back to Home</a>
            <% } %>
          <%})%>
        </form>
      </div>
    </main>
  </body>
</html>
