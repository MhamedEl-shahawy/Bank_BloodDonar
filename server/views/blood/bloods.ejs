
    <%- include('../includes/navigation.ejs') %>
  <section class="main">
    <div class="main-content tabcontent" id="availabel">
      <!-- start new item -->
       
      <!-- end new item -->
      <div class="pd-1">
    <div class="container" >
       <button class="add-new addMain">new</button>
      <table class="availabel">
     <thead>
       <tr>
         <th scope="col">blod</th>
         <th scope="col">quantity</th>
         <th scope="col"></th>
       </tr>
     </thead>
     <tbody>
      <% if (bloods.length > 0) {
        let i = itemCounterStartInCurrentPage;
        for (let blood of bloods) { %>
       <tr>
         <td data-label="name"><%= blood.bloodType %></td>
         <td data-label="quantity"> <%= blood.bloodQuantity %></td>
       

         <td data-label="edit" class="right">
           <% if (isAuthenticated && blood.userId._id.toString() === currentUser._id.toString()) { %>
           <span><a href="/edit-blood/<%= blood._id %>?edit=true"><i class="fas fa-edit"></i></a></span>
           <span>
             <form action="delete-blood" method="POST" style="display: inline-block;">
              <input type="hidden" value="<%= blood._id %>" name="bloodId">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <a type="submit"><i class="fas fa-trash-alt"></i></a>
            </form>
          </span>
        </td>
            <% } %> 


         
       
        </tr> 
        <% i--; } %>
      <% } else { %>
        <p>No Patients found.</p>
        <% } %>
      </tbody>
  </table>
    </div> 		

</div>
</div> 
  </section>









<!-- 
  <main class="myContainer">
    <% if (bloods.length > 0) {
      let i = itemCounterStartInCurrentPage;
      for (let blood of bloods) { %>
        <li class="myBox">
          <div class="myItem">          
              <p><%= i %></p>
            </div>
          
          <p>Blood Type</p>
          <div class="myItem">
            <%= blood.bloodType %>
          </div>  

          <p>Quantity:</p>
          <div class="myItem">
            <%= blood.bloodQuantity %>
          </div>

          <p>Date</p>
          <div class="myItem">
            <%=blood.date%>
          </div>

          <% if (isAuthenticated && blood.userId._id.toString() === currentUser._id.toString()) { %>
          <div class="myItem">
            <a href="/edit-blood/<%= blood._id %>?edit=true" class="btn">Edit</a>
          </div>
          <div class="myItem">
            <form action="delete-blood" method="POST">
              <input type="hidden" value="<%= blood._id %>" name="bloodId">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button class="myButtonThatLooksLikeLink" type="submit">Delete</button>
            </form>
            <% } %>
          </div>
        </li>
    <% i--; } %>

    <% } else { %>
    <p>No Bloods found.</p>
    <% } %>

    <%- include('../includes/pagination.ejs', paginationObject) %>

  </main> -->

  <%- include('../includes/bottom.ejs') %>